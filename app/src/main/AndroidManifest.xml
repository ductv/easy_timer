<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 1. Quyền đặt báo thức chính xác (Bắt buộc từ Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 2. Quyền hiển thị thông báo (Bắt buộc từ Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 3. Quyền tự khởi động lại báo thức sau khi điện thoại reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 4. (Tùy chọn) Quyền rung điện thoại khi báo thức -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Quyền hiện đè lên màn hình khóa -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyTimer">

        <receiver android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="false"
            />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.AlarmActivity"
            android:showOnLockScreen="false"
            android:exported="true"
            android:launchMode="singleInstance" />
    </application>

</manifest>